export function HeroBackdrop() {
  return (
    <div className="hero-backdrop" aria-hidden="true">
      <span className="hero-blob hero-blob--teal" />
      <span className="hero-blob hero-blob--magenta" />
      <span className="hero-blob hero-blob--mustard" />
    </div>
  );
}
